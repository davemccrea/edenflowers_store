# Eden Flowers

## Create

```elixir
fulfillment_option = Edenflowers.Store.FulfillmentOption
|> Ash.Changeset.for_create(:create, %{
  name: "Hemleverans",
  type: :dynamic,
  base_price: 3000,
  price_per_km: 150,
  free_dist_km: 5,
  max_dist_km: 20,
})
|> Ash.create!()

Edenflowers.Store.Product
|> Ash.Changeset.for_create(:create, %{title: "My Product", description: "My product description"})
|> Ash.create!()

Edenflowers.Store.Product
|> Ash.Query.for_read(:by_title, %{title: "Product 1 title"})
|> Ash.read_one()
```

## Read

```elixir
Edenflowers.Store.FulfillmentOption
|> Ash.read_first!()
```
